# 服务器端口
PORT=3000

# JWT 签名密钥（部署时随机生成，示例：openssl rand -base64 32）
JWT_SECRET=请在此填入随机生成的密钥

# JWT 有效期
JWT_EXPIRES_IN=2h

# MySQL 数据库连接配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=请在此填入数据库密码
DB_NAME=meeting_vote
