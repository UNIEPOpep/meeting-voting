# Meeting Voting - 产品设计文档

---

## 一、产品概述

一个会议投票系统，支持发起投票、参会投票、数据汇总。

**选定的技术方案**：Kotlin 原生安卓 + Node.js 后端 + MySQL 数据库

---

## 二、用户角色与权限

系统共有三种角色，权限逐级包含：

### 角色一览

| 权限 | 普通用户 | 普通管理员 | 超级管理员 |
|------|:---:|:---:|:---:|
| 登录 | ✅ | ✅ | ✅ |
| 参与投票（同意/反对/弃权） | ✅ | ✅ | ✅ |
| 发起投票 | ❌ | ✅ | ✅ |
| 设置投票截止时间 | ❌ | ✅ | ✅ |
| 设置是否允许弃权 | ❌ | ✅ | ✅ |
| 设置是否允许修改投票 | ❌ | ✅ | ✅ |
| 查看自己发起的投票汇总 | ❌ | ✅ | ✅ |
| 修改自己的解锁密钥 | ❌ | ✅ | ✅ |
| 创建新用户 | ❌ | ❌ | ✅ |
| 创建/管理普通管理员 | ❌ | ❌ | ✅ |

### 角色说明

**普通用户**（登录后的默认角色）
- 登录 / 退出登录
- 参与投票：输入文件编号 + 投票密码，选择同意 / 反对 / 弃权（如果该投票允许弃权）

**普通管理员**
- 拥有普通用户全部权限
- 发起投票（可设置是否允许弃权）
- 查看自己发起投票的数据汇总
- 修改自己的解锁密钥
- **不能创建用户**

**超级管理员**
- 拥有普通管理员全部权限
- 创建新用户（普通用户 + 普通管理员）
- 查看所有投票的数据汇总
- 修改系统解锁密钥
- **整个系统只有 1 个超级管理员**，部署时预设

```
角色层级：  超级管理员  >  普通管理员  >  普通用户
              （唯一）      （多个）       （多个）
```

---

## 三、功能详细说明

### 1. 登录
- 使用账号 + 密码登录
- 登录后默认是普通用户权限，只能投票
- 系统部署时预置 1 个超级管理员账号
- 超管登录后创建普通管理员和普通用户账号

### 2. 权限解锁（管理员）
- 普通管理员 / 超管在管理页输入解锁密钥
- 验证通过后解锁对应管理功能
- 退出登录后权限失效，重新登录需再次输入密钥

### 3. 发起投票（普通管理员 + 超管）
- **输入项**：
  - 投票主题（如"2026年度预算审批"，必填）
  - 文件编号（如 ABC001）
  - 投票密码（如 123456）
  - **投票截止时间**（日期+时间，如"2026-07-30 18:00"，必填）
  - **是否允许弃权**（开关，默认开启）
  - **是否允许修改投票**（开关，默认关闭）；开启后投票人可在截止前改票
- 点击"发起投票"按钮，创建投票会话
- 发起人自己也能参与该投票
- 通过线下方式（口头/微信等）将编号和密码告知参会人员

### 4. 被动投票（所有用户）
- 输入文件编号 + 投票密码进入投票
- 投票选项：
  - ✅ 同意
  - ❌ 反对
  - ⏸️ 弃权（仅当发起人开启了"允许弃权"时显示）
- 每人每个投票只能投一次
- **超过截止时间后不能投票**
- **改票规则**：如果发起人开启了"允许修改投票"，投票人可在截止时间前重新进入投票并修改选择；如果未开启，提交后不可修改

### 5. 数据汇总（普通管理员 + 超管）
- **普通管理员**：只能看到自己发起的投票汇总
- **超级管理员**：可以看到所有人的投票汇总
- 汇总内容：
  - 文件编号
  - 投票总人数
  - 同意票数 / 百分比
  - 反对票数 / 百分比
  - 弃权票数 / 百分比
  - 各用户投票明细

### 6. 创建用户（仅超管）
- 输入：账号 + 密码 + 角色（普通用户 / 普通管理员）
- 创建后该用户即可登录使用

### 7. 修改密钥（管理员）
- 输入旧密钥 → 输入新密钥 → 确认
- 超管可修改全局解锁密钥

---

## 四、页面结构

```
登录页
  ├── 账号
  ├── 密码
  └── [登录]

首页（投票大厅）
  ├── 投票区（所有人可见）
  │     ├── 文件编号输入框
  │     ├── 投票密码输入框
  │     ├── [进入投票]
  │     └── 投票选项：同意 / 反对 / 弃权（如有）→ [提交]
  │
  ├── 发起投票区（管理员可见，需解锁）
  │     ├── 投票主题输入框（必填）
  │     ├── 文件编号输入框
  │     ├── 投票密码输入框
  │     ├── 投票截止时间 [日期+时间选择器]（必填）
  │     ├── 允许弃权 [开关]
  │     ├── 允许修改投票 [开关]
  │     └── [发起投票]
  │
  └── 我的投票记录

管理页（需输入解锁密钥）
  ├── [密钥输入框] [解锁]（未解锁时显示）
  │
  ├── 发起投票入口（解锁后可见）
  ├── 数据汇总列表（解锁后可见，超管看全部/普通管理员看自己的）
  ├── 创建用户（仅超管可见）
  └── 修改密钥（解锁后可见）

个人中心
  ├── 当前账号信息
  └── [退出登录]
```

---

## 五、技术方案 — 已选定

### Kotlin 原生安卓 + Node.js 后端 ✅ 已选定

| 项目 | 技术 |
|------|------|
| APP前端 | Kotlin + Jetpack Compose（安卓原生开发） |
| 后端服务 | Node.js + Express（JavaScript） |
| 数据库 | MySQL |
| 开发工具 | Android Studio 或 Gradle 命令行 |
| 服务器 | 需云服务器一台（阿里云/腾讯云，约50元/月起） |

**✅ 优势**：
- 纯原生安卓，性能最佳，体验最流畅
- 后端用 JavaScript（Node.js），开发速度快
- 数据完全在自己服务器上，安全性高
- 国内访问无障碍

**后端已开发完成**（server/ 目录），接下来只需开发安卓APP端。

---

## 六、开发约定 ⚠️

> **本项目最重要的约定，贯穿始终：**

1. **用户不懂编程。** 任何技术决策（选框架、选数据库、设计接口、选第三方库、部署方式等），Claude 必须提供 **2-3 个方案**，用通俗语言解释每个方案的优劣，由用户决定。

2. 不能假设用户的技术偏好。不能因为某个方案"大家都这么用"就直接替用户决定。

3. 开发分阶段推进，每阶段开始前先和用户确认：
   - ✅ 第1步：选定技术方案 — **已完成（Kotlin原生安卓 + Node.js）**
   - ✅ 第2步：数据库表设计与API接口设计 — **已完成**
   - ✅ 第3步：搭建项目骨架（后端 + APP）— **已完成**
   - ✅ 第4步：逐个实现功能模块 — **已完成**
   - 🔜 第5步：配置开发环境，编译测试
   - 第6步：打包交付

4. 所有技术术语必须用通俗语言解释，不假设用户知道任何技术名词。

---

## 七、第2步：数据库与接口设计

### 7.1 需要你决定的技术选型

在开始设计之前，有一个技术选择需要你决定：

---

#### 用户登录状态保持方案

APP登录后，需要一种方式来"记住"用户已经登录了。有两种主流方案：

| | 方案1：JWT 令牌 | 方案2：Session 会话 |
|:--|:---|:---|
| **怎么理解** | 登录后服务器发一个"电子身份证"（令牌），APP每次请求都带上它，服务器验证真伪 | 登录后服务器在内存中记录"这个人登录了"，APP带一个编号，服务器查表确认 |
| **服务器压力** | ✅ 小，不需要记状态 | ⚠️ 用户多了内存占用大 |
| **APP适配** | ✅ 移动APP标配方案 | ⚠️ 处理稍复杂 |
| **安全性** | ✅ 令牌可设有效期和刷新机制 | ⚠️ 依赖服务器，服务重启可能丢失 |
| **业界使用** | 主流方案，绝大多数APP使用 | 传统方案，主要用于网站 |
| **推荐** | 🔥 推荐 | |

> **通俗比喻**：JWT就像一张带防伪标识的通行证，谁拿着都能用（只要防伪是真的）；Session就像保安手里的小本本，来一个人就登记一下。

---

**已选择：方案1 — JWT 令牌** ✅

---

### 7.2 数据库表设计

需要 **4张表**。下面用通俗方式解释每张表存什么：

---

#### 📋 表1：users（用户表）

存储所有用户账号信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | 数字（自增） | 用户唯一ID，系统自动生成 |
| username | 文字 | 登录账号，不能重复 |
| password_hash | 文字 | 密码的加密结果（不是明文存储） |
| role | 文字 | 角色：`super_admin` / `admin` / `user` |
| created_at | 日期时间 | 账号创建时间 |
| created_by | 数字 | 创建人ID（谁创建的此账号） |

> **为什么密码不存明文？** 明文存密码就像把银行卡密码写在卡上，泄露后所有人都能登录。我们会用 **bcrypt** 算法把密码变成一串看不懂的乱码（如 `$2b$10$xxx...`），即使数据库被偷，密码也不会泄露。

---

#### 📋 表2：voting_sessions（投票会话表）

存储每一次投票。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | 数字（自增） | 投票唯一ID |
| topic | 文字 | 投票主题，如"2026年度预算审批" |
| file_number | 文字 | 文件编号，如 ABC001 |
| vote_password_hash | 文字 | 投票密码的加密结果 |
| deadline | 日期时间 | 投票截止时间 |
| allow_abstain | 是/否 | 是否允许投弃权票 |
| allow_change_vote | 是/否 | 是否允许修改已投的票 |
| created_by | 数字 | 发起人ID |
| created_at | 日期时间 | 投票创建时间 |

---

#### 📋 表3：votes（投票记录表）

存储每个人的投票选择。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | 数字（自增） | 记录ID |
| session_id | 数字 | 属于哪个投票 |
| user_id | 数字 | 谁投的 |
| choice | 文字 | 投票选择：`agree`（同意）/ `oppose`（反对）/ `abstain`（弃权） |
| voted_at | 日期时间 | 投票时间 |
| updated_at | 日期时间 | 改票时间（没改过则为空） |

> **规则**：session_id + user_id 设唯一约束，确保同一人同一投票只能有一条记录。

---

#### 📋 表4：system_config（系统配置表）

存储系统全局设置。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| config_key | 文字 | 配置名，如 `admin_secret_key` |
| config_value | 文字 | 配置值，如解锁密钥的加密结果 |

---

### 7.3 四张表之间的关系

```
users（用户表）
  │
  ├──→ voting_sessions（投票会话表）
  │      created_by → users.id（谁发起的）
  │
  └──→ votes（投票记录表）
         user_id → users.id（谁投的）
         session_id → voting_sessions.id（投的哪个票）

system_config（系统配置表）独立存在，存全局设置
```

---

### 7.4 API 接口设计

APP和后端通过 **HTTP 请求** 通信。通俗理解：APP"打电话"给服务器，告诉服务器要做什么，服务器返回结果。

所有接口地址以 `/api` 开头。需要登录的接口要在请求头里带上 JWT 令牌。

---

#### 🔑 登录相关

| 方法 | 地址 | 说明 | 需要登录 |
|------|------|------|:---:|
| POST | `/api/auth/login` | 用户登录，返回JWT令牌 | ❌ |
| GET | `/api/auth/me` | 获取当前登录用户的信息 | ✅ |

**登录请求示例**：
```
APP发：{ "username": "zhangsan", "password": "123456" }
服务器回：{ "token": "eyJhbG...", "user": { "id": 1, "username": "zhangsan", "role": "user" } }
```

---

#### 👤 用户管理（仅超管）

| 方法 | 地址 | 说明 | 需要登录 |
|------|------|------|:---:|
| POST | `/api/users` | 创建新用户 | ✅ 超管 |
| GET | `/api/users` | 获取用户列表 | ✅ |

---

#### 🔐 管理权限

| 方法 | 地址 | 说明 | 需要登录 |
|------|------|------|:---:|
| POST | `/api/admin/unlock` | 输入密钥，获取管理员操作权限 | ✅ |
| PUT | `/api/admin/secret-key` | 修改解锁密钥（需旧密钥） | ✅ 超管 |

> **说明**：`/api/admin/unlock` 验证密钥后，服务器返回一个升级后的JWT令牌（内含管理员权限标记），后续操作用新令牌。

---

#### 📮 投票操作

| 方法 | 地址 | 说明 | 需要登录 |
|------|------|------|:---:|
| POST | `/api/voting-sessions` | 发起投票 | ✅ 管理员 |
| GET | `/api/voting-sessions` | 获取投票列表（用于汇总） | ✅ |
| POST | `/api/voting-sessions/join` | 通过文件编号+密码进入投票 | ✅ |
| GET | `/api/voting-sessions/:id` | 获取单个投票的详情和结果 | ✅ |
| POST | `/api/voting-sessions/:id/vote` | 提交投票选择 | ✅ |
| PUT | `/api/voting-sessions/:id/vote` | 修改投票选择 | ✅ |

**发起投票请求示例**：
```
APP发：{
  "topic": "2026年度预算审批",
  "file_number": "ABC001",
  "vote_password": "123456",
  "deadline": "2026-07-30T18:00:00",
  "allow_abstain": true,
  "allow_change_vote": false
}
服务器回：{ "id": 1, "message": "投票创建成功" }
```

**加入投票请求示例**：
```
APP发：{ "file_number": "ABC001", "vote_password": "123456" }
服务器回：{ "session_id": 1, "topic": "2026年度预算审批", "deadline": "2026-07-30T18:00:00", ... }
```

**提交投票请求示例**：
```
APP发：{ "choice": "agree" }    // agree / oppose / abstain
服务器回：{ "message": "投票成功" }
```

**汇总数据返回示例**：
```
服务器回：{
  "topic": "2026年度预算审批",
  "file_number": "ABC001",
  "total": 20,
  "agree": { "count": 15, "percent": "75%" },
  "oppose": { "count": 3, "percent": "15%" },
  "abstain": { "count": 2, "percent": "10%" },
  "details": [
    { "username": "zhangsan", "choice": "agree", "voted_at": "..." },
    { "username": "lisi", "choice": "oppose", "voted_at": "..." }
  ]
}
```

---

### 7.5 接口总览图

```
APP ───────────────────── 后端服务器 ──────────────── MySQL数据库
│                              │                         │
├─ POST /api/auth/login ──────→ 验证账号密码 ──────────→ users表
│                               ← 返回JWT令牌
│
├─ POST /api/admin/unlock ────→ 验证密钥 ──────────────→ system_config表
│                               ← 返回升级令牌
│
├─ POST /api/voting-sessions ─→ 创建投票 ────────────→ voting_sessions表
│
├─ POST /api/voting-sessions/join → 验证编号+密码 ───→ voting_sessions表
│                               ← 返回投票详情
│
├─ POST /api/voting-sessions/:id/vote → 提交投票 ──→ votes表
│
├─ GET /api/voting-sessions ───→ 查汇总数据 ──────→ voting_sessions + votes表
│                               ← 返回统计数据
│
├─ POST /api/users ───────────→ 创建用户 ────────────→ users表
│
└─ PUT /api/admin/secret-key ─→ 修改密钥 ────────────→ system_config表
```

---

### 7.6 规则校验

后端在以下情况会返回错误提示：

| 场景 | 返回的错误 |
|------|-----------|
| 账号或密码错误 | "用户名或密码错误" |
| 投票截止时间已过 | "投票已截止" |
| 重复投票 | "您已投过票了" |
| 不允许改票 | "该投票不允许修改" |
| 文件编号或投票密码错误 | "文件编号或投票密码错误" |
| 无权操作 | "权限不足" |
| 密钥错误 | "密钥错误" |

---

## 八、设计确认

以上是第2步的完整设计。请确认以下几点：

1. ✅ 4张数据库表的设计是否合理
2. ✅ API接口是否覆盖了所有功能
3. ✅ 是否有遗漏的功能或需要调整的地方

确认无误后，我进入第3步——搭建项目骨架，开始写代码。
